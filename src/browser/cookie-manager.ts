/**
 * @deprecated Superseded by Playwright launchPersistentContext.
 * Session is now stored automatically inside ~/.xhs-pro-mcp/chrome-profile/.
 * This file is kept as an empty stub so existing imports don't break during
 * migration — safe to delete once all callers are removed.
 */
export {};
